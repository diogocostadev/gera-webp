@{
    ViewData["Title"] = "Free Online PNG Converter - Convert Images to PNG";
    ViewData["Description"] = "Free and fast online PNG converter. Convert JPG, GIF, WebP images to PNG format with transparency support. Optimize your images for web in seconds!";
    ViewData["Keywords"] = "png converter, convert to png, jpg to png, gif to png, webp to png, image converter";
}

<!-- SEO Hero Section -->
<section class="hero-seo">
    <div class="container">
        <header class="hero-content">
            <h1>Online PNG Image Converter</h1>
            <p class="hero-description">
                <strong>Convert your images to PNG format online</strong> for free and get maximum quality
                with complete transparency support. Ideal for logos, icons and professional graphics.
            </p>
            <div class="hero-features">
                <span class="feature-badge">🔄 Transparency Support</span>
                <span class="feature-badge">🔒 Safe & Private</span>
                <span class="feature-badge">👌 High Quality</span>
                <span class="feature-badge">📦 Batch</span>
            </div>
        </header>
    </div>
</section>

<!-- Service Notice -->
<div class="free-service-notice">
    <div class="container">
        <div class="notice-content">
            <i data-feather="layers"></i>
            <span><strong>PNG Converter</strong> • Transparency Preserved • Maximum Quality</span>
        </div>
    </div>
</div>

<!-- Main Converter -->
<main class="main-converter">
    <div class="converter-container">
        <form id="converterForm" method="post" action="/en/convert-to-png" enctype="multipart/form-data">
            
            <!-- Breadcrumbs -->
            <nav aria-label="breadcrumb" class="breadcrumb-nav">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/en">Home</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Convert to PNG</li>
                </ol>
            </nav>
            
            <!-- Conversion Workflow -->
            <div class="converter-workflow">
                <!-- Step 1: Upload -->
                <div class="workflow-step upload-step">
                    <div class="step-header">
                        <div class="step-number">1</div>
                        <h2>Select Images</h2>
                    </div>
                    <div class="upload-area" id="uploadArea">
                        <div class="upload-icon" style="color: var(--primary-color);">
                            <i data-feather="upload"></i>
                        </div>
                        <p>Drag images or click to select</p>
                        <div class="upload-button" style="background: var(--primary-color);">
                            <i data-feather="folder"></i>
                            Choose Images
                        </div>
                        <div class="upload-info">
                            <small>JPG, GIF, WebP • Max: 100MB per file or 100MB total</small>
                        </div>
                        <label for="arquivos" class="visually-hidden">Select images for conversion</label>
                        <input type="file" name="arquivos" id="arquivos" multiple="multiple" class="file-input" 
                               accept="image/jpeg,image/jpg,image/gif,image/webp" />
                    </div>
                    
                    <!-- Selected Files Display -->
                    <div id="selectedFiles" class="selected-files" style="display: none;">
                        <div class="files-header">
                            <i data-feather="check-circle"></i>
                            <span id="fileCount">0</span> image(s) selected
                        </div>
                    </div>
                </div>

                <!-- Step 2: Quality Settings -->
                <div class="workflow-step quality-step">
                    <div class="step-header">
                        <div class="step-number">2</div>
                        <h2>Configure PNG Quality</h2>
                    </div>
                    
                    <div class="quality-control">
                        <label for="qualidade" class="quality-label">
                            PNG Quality: <strong id="qualidadeValue">75</strong>
                        </label>
                        <input type="range" id="qualidade" name="qualidade" min="30" max="95" value="75" 
                               class="quality-slider" oninput="atualizarValorDaQuantidade(this.value)">
                        <div class="quality-marks">
                            <span class="quality-mark low">Maximum Compression</span>
                            <span class="quality-mark high">Maximum Quality</span>
                        </div>
                    </div>
                    
                    <div class="compression-presets">
                        <h4>Quality Presets:</h4>
                        <div class="preset-buttons">
                            <button type="button" class="preset-btn" data-preset="90" data-tooltip="High quality, larger file">
                                <i data-feather="award"></i>
                                High Quality
                            </button>
                            <button type="button" class="preset-btn active" data-preset="75" data-tooltip="Balance between quality and size">
                                <i data-feather="target"></i>
                                Recommended
                            </button>
                            <button type="button" class="preset-btn" data-preset="50" data-tooltip="High compression, smaller file">
                                <i data-feather="zap"></i>
                                Optimized
                            </button>
                        </div>
                    </div>
                    
                    <div class="quality-preview">
                        <div class="preview-item">
                            <div class="preview-icon">
                                <i data-feather="layers"></i>
                            </div>
                            <div class="preview-text">
                                <strong>Final format:</strong>
                                <small>PNG with transparency</small>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Step 3: Convert -->
                <div class="workflow-step convert-step">
                    <div class="step-header">
                        <div class="step-number">3</div>
                        <h2>Convert to PNG</h2>
                    </div>
                    
                    <button type="submit" class="convert-button" id="convertButton" aria-label="Convert images to PNG">
                        <i data-feather="layers"></i>
                        Convert to PNG Now
                    </button>
                    
                    <div class="convert-info">
                        <small>PNG conversion with full transparency support</small>
                    </div>
                </div>
            </div>

            <!-- Progress & Download Sections -->
            <div id="progressContainer" class="progress-section" style="display: none;">
                <h3>Converting your images...</h3>
                <div class="progress-bar">
                    <div class="progress-fill" id="progress-fill"></div>
                </div>
                <p id="progressText">0%</p>
            </div>

            @if (ViewBag.DownloadLink != null)
            {
                <div class="download-section">
                    <div class="download-success">
                        <i data-feather="check-circle"></i>
                        <h3>Images Converted to PNG!</h3>
                        <p>Your images have been successfully converted to PNG format.</p>
                        <a href="@ViewBag.DownloadLink" class="download-button" download aria-label="Download converted images">
                            <i data-feather="download"></i>
                            Download PNG Images
                        </a>
                        <small>ZIP file with all images in PNG format</small>
                    </div>
                </div>
            }
        </form>
    </div>
</main>

<!-- SEO Content -->
<section class="seo-content">
    <div class="container">
        
        <!-- Benefits Section -->
        <section class="conversion-benefits" aria-labelledby="benefits-title">
            <h2 id="benefits-title" class="section-title">Why Convert Images to PNG?</h2>
            
            <div class="benefits-grid">
                <article class="benefit-item">
                    <div class="benefit-icon">
                        <i data-feather="layers"></i>
                    </div>
                    <h3>Full Transparency Support</h3>
                    <p>PNG completely preserves transparent areas, ideal for logos, icons and professional graphics with transparent backgrounds.</p>
                </article>
                
                <article class="benefit-item">
                    <div class="benefit-icon">
                        <i data-feather="award"></i>
                    </div>
                    <h3>Lossless Quality</h3>
                    <p>PNG format maintains perfect quality without destructive compression. Ideal for images with text, sharp edges and details.</p>
                </article>
                
                <article class="benefit-item">
                    <div class="benefit-icon">
                        <i data-feather="monitor"></i>
                    </div>
                    <h3>Universal Compatibility</h3>
                    <p>PNG is widely supported by all browsers, graphic editors and platforms. Standard for web and design.</p>
                </article>
                
                <article class="benefit-item">
                    <div class="benefit-icon">
                        <i data-feather="palette"></i>
                    </div>
                    <h3>Perfect for Design</h3>
                    <p>Perfect for logos, icons, illustrations and any image that needs to maintain transparency and maximum quality.</p>
                </article>
            </div>
        </section>

        <!-- How-to Section -->
        <section class="howto-section" aria-labelledby="howto-title">
            <h2 id="howto-title" class="section-title">How to Convert Images to PNG Online</h2>
            
            <div class="steps-container">
                <div class="step-item">
                    <div class="step-number">1</div>
                    <h3>Upload Images</h3>
                    <p>Select or drag JPG, GIF or WebP images you want to convert to PNG. We support multiple files simultaneously.</p>
                </div>
                
                <div class="step-item">
                    <div class="step-number">2</div>
                    <h3>Adjust Quality</h3>
                    <p>Configure PNG quality level: high quality for professional use or optimized to reduce file size.</p>
                </div>
                
                <div class="step-item">
                    <div class="step-number">3</div>
                    <h3>Smart Conversion</h3>
                    <p>Our technology converts to PNG preserving transparency and applying intelligent compression without quality loss.</p>
                </div>
                
                <div class="step-item">
                    <div class="step-number">4</div>
                    <h3>Instant Download</h3>
                    <p>Download your PNG converted images in ZIP. Ready for use in websites, applications and graphic projects!</p>
                </div>
            </div>
        </section>

        <!-- FAQ Section -->
        <section class="faq-section" aria-labelledby="faq-title">
            <h2 id="faq-title" class="section-title">Frequently Asked Questions about PNG Conversion</h2>
            
            <div class="faq-container">
                <details class="faq-item">
                    <summary>Why convert to PNG instead of other formats?</summary>
                    <p>PNG offers lossless quality with full transparency support, being ideal for logos, icons and images that need to maintain sharp details and transparent areas.</p>
                </details>
                
                <details class="faq-item">
                    <summary>Is transparency preserved in PNG conversion?</summary>
                    <p>Yes! PNG natively supports transparency and our conversion completely preserves all transparent areas from original images.</p>
                </details>
                
                <details class="faq-item">
                    <summary>What's the difference between PNG and other image formats?</summary>
                    <p>PNG is lossless (preserves original quality), supports transparency and is ideal for graphics. JPG is smaller but lossy, WebP is modern but with less compatibility.</p>
                </details>
                
                <details class="faq-item">
                    <summary>Can I convert many images to PNG at once?</summary>
                    <p>Yes! Our converter supports batch conversion up to 100MB total. Convert dozens of images to PNG simultaneously.</p>
                </details>
                
                <details class="faq-item">
                    <summary>Is PNG suitable for websites and web applications?</summary>
                    <p>Absolutely! PNG is widely supported, ideal for logos, icons and images with transparency in modern websites and applications.</p>
                </details>
            </div>
        </section>

        <!-- CTA Section -->
        <section class="cta-section">
            <div class="cta-content">
                <h2>Convert Your Images to PNG Now!</h2>
                <p>Get maximum quality and transparency by converting to PNG for free.</p>
                <a href="#uploadArea" class="cta-button" aria-label="Start PNG conversion">
                    <i data-feather="layers"></i>
                    Start PNG Conversion
                </a>
            </div>
        </section>

        <!-- Other Tools -->
        @await Html.PartialAsync("_ToolsGrid")
    </div>
</section>

<style>
        /* CSS específico para a página PNG */
        .conversion-benefits .benefits-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
            margin-bottom: 3rem;
        }
        
        .conversion-benefits .benefit-item {
            background: white;
            padding: 2.5rem 2rem;
            border-radius: var(--radius-lg);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            text-align: center;
            transition: all 0.3s ease;
            border: 1px solid var(--border-color);
            min-height: 280px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        
        .conversion-benefits .benefit-item:hover {
            transform: translateY(-8px);
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
            border-color: var(--primary-color);
        }
        
        .conversion-benefits .benefit-icon {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, var(--primary-color), #0056b3);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            color: white;
            font-size: 2rem;
        }
        
        .conversion-benefits .benefit-item h3 {
            color: var(--text-primary);
            margin-bottom: 1rem;
            font-size: 1.25rem;
            font-weight: 600;
        }
        
        .conversion-benefits .benefit-item p {
            color: var(--text-secondary);
            line-height: 1.6;
            font-size: 0.95rem;
            margin: 0;
        }
        
        .howto-section .steps-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-bottom: 3rem;
        }
        
        .howto-section .step-item {
            background: white;
            padding: 2rem 1.5rem;
            border-radius: var(--radius-lg);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            text-align: center;
            transition: all 0.3s ease;
            border: 1px solid var(--border-color);
            min-height: 220px;
            display: flex;
            flex-direction: column;
            justify-content: flex-start;
            align-items: center;
        }
        
        .howto-section .step-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
        }
        
        .howto-section .step-number {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, var(--primary-color), #0056b3);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            font-weight: 700;
            margin: 0 auto 1.5rem;
        }
        
        .howto-section .step-item h3 {
            color: var(--text-primary);
            margin-bottom: 1rem;
            font-size: 1.15rem;
            font-weight: 600;
        }
        
        .howto-section .step-item p {
            color: var(--text-secondary);
            line-height: 1.6;
            font-size: 0.9rem;
            margin: 0;
        }
        
        .compression-presets {
            margin: 1.5rem 0;
            text-align: center;
        }
        
        .compression-presets h4 {
            margin-bottom: 1rem;
            color: var(--text-secondary);
            font-size: 0.9rem;
        }
        
        .preset-buttons {
            display: flex;
            gap: 0.75rem;
            justify-content: center;
            flex-wrap: wrap;
        }
        
        .preset-btn {
            padding: 0.75rem 1.25rem;
            border: 2px solid var(--border-color);
            background: white;
            border-radius: var(--radius-md);
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.9rem;
            font-weight: 500;
            min-width: 140px;
            justify-content: center;
        }
        
        .preset-btn:hover {
            border-color: var(--primary-color);
            background: rgba(var(--primary-color-rgb), 0.05);
            transform: translateY(-2px);
        }
        
        .preset-btn.active {
            border-color: var(--primary-color);
            background: var(--primary-color);
            color: white;
            box-shadow: 0 4px 15px rgba(var(--primary-color-rgb), 0.3);
        }
        
        /* Responsivo */
        @@media (max-width: 1024px) {
            .conversion-benefits .benefits-grid {
                grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                gap: 1.5rem;
            }
            
            .howto-section .steps-container {
                grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
                gap: 1.5rem;
            }
        }
        
        @@media (max-width: 768px) {
            .conversion-benefits .benefits-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }
            
            .howto-section .steps-container {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }
            
            .conversion-benefits .benefit-item {
                padding: 2rem 1.5rem;
                min-height: 240px;
            }
            
            .howto-section .step-item {
                padding: 1.5rem 1rem;
                min-height: 200px;
            }
            
            .preset-buttons {
                flex-direction: column;
                align-items: center;
                gap: 0.5rem;
            }
            
            .preset-btn {
                min-width: 200px;
            }
        }
        
        @@media (max-width: 480px) {
            .conversion-benefits .benefit-icon {
                width: 60px;
                height: 60px;
                font-size: 1.5rem;
            }
            
            .howto-section .step-number {
                width: 50px;
                height: 50px;
                font-size: 1.25rem;
            }
        }
    </style>

@section Scripts {
    <script>
        feather.replace();
        
        function atualizarValorDaQuantidade(value) {
            document.getElementById('qualidadeValue').textContent = value;
            updateActivePreset(value);
        }
        
        function updateActivePreset(value) {
            const presetButtons = document.querySelectorAll('.preset-btn');
            presetButtons.forEach(btn => btn.classList.remove('active'));
            
            const targetPreset = presetButtons[1]; // Default to middle preset
            if (value <= 60) presetButtons[2]?.classList.add('active');
            else if (value >= 85) presetButtons[0]?.classList.add('active');
            else presetButtons[1]?.classList.add('active');
        }

        // File upload handling
        const fileInput = document.getElementById('arquivos');
        const uploadArea = document.getElementById('uploadArea');
        const selectedFiles = document.getElementById('selectedFiles');
        const fileCount = document.getElementById('fileCount');

        fileInput.addEventListener('change', function(e) {
            updateFileDisplay(e.target.files);
        });

        uploadArea.addEventListener('click', () => fileInput.click());

        // Drag and drop
        uploadArea.addEventListener('dragover', (e) => {
            e.preventDefault();
            uploadArea.classList.add('drag-over');
        });

        uploadArea.addEventListener('dragleave', () => {
            uploadArea.classList.remove('drag-over');
        });

        uploadArea.addEventListener('drop', (e) => {
            e.preventDefault();
            uploadArea.classList.remove('drag-over');
            const files = Array.from(e.dataTransfer.files).filter(file => 
                ['image/jpeg', 'image/jpg', 'image/gif', 'image/webp'].includes(file.type)
            );
            if (files.length > 0) {
                const dt = new DataTransfer();
                files.forEach(file => dt.items.add(file));
                fileInput.files = dt.files;
                updateFileDisplay(files);
            }
        });

        // Preset buttons
        document.querySelectorAll('.preset-btn').forEach(btn => {
            btn.addEventListener('click', function() {
                const preset = this.dataset.preset;
                document.getElementById('qualidade').value = preset;
                atualizarValorDaQuantidade(preset);
            });
        });

        function updateFileDisplay(files) {
            if (files.length > 0) {
                let totalSize = 0;
                for (let i = 0; i < files.length; i++) {
                    totalSize += files[i].size;
                }
                const totalMB = (totalSize / (1024 * 1024)).toFixed(1);
                
                fileCount.textContent = `${files.length} image(s) • ${totalMB}MB total`;
                selectedFiles.style.display = 'block';
                uploadArea.classList.add('has-files');
            } else {
                selectedFiles.style.display = 'none';
                uploadArea.classList.remove('has-files');
            }
        }

        // Form submission with progress
        document.getElementById('converterForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const formData = new FormData(this);
            const progressContainer = document.getElementById('progressContainer');
            const convertButton = document.getElementById('convertButton');
            
            progressContainer.style.display = 'block';
            convertButton.disabled = true;
            convertButton.innerHTML = '<i data-feather="loader"></i> Converting...';
            feather.replace();
            
            fetch('/api/converter-para-png', {
                method: 'POST',
                body: formData
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    // Redirecionar para download
                    window.location.href = data.downloadLink;
                } else {
                    alert('Error: ' + data.message);
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('Conversion error. Please try again.');
            })
            .finally(() => {
                progressContainer.style.display = 'none';
                convertButton.disabled = false;
                convertButton.innerHTML = '<i data-feather="layers"></i> Convert to PNG Now';
                feather.replace();
            });
        });
    </script>
    <script src="~/js/site.js"></script>
} 
